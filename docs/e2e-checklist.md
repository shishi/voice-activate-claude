# E2E 手動チェックリスト(Windows実機)

前提: `uv sync` 済み、初回モデルダウンロード済み(下記「初回セットアップ」)、
`uv run python -m vac` で常駐中。

## 初回セットアップ(初めての実行前に一度だけ)

- [ ] `uv run python -m vac.check wake` を実行し、openwakewordモデルをダウンロードする
- [ ] `uv run python -m vac.check whisper` を実行し、Whisperモデルをダウンロードする
- [ ] 注意: モデル未ダウンロードのまま常駐起動すると5秒毎のエラー音ループになる(原因はvac.logに出る)
- [ ] `uv run python -m vac.check devices` でマイク一覧を確認し、既定が使いたいマイクでなければ config の `input_device`(または各checkの `--device`)で指定する

## 正常系

- [ ] 「hey jarvis」(またはカスタムワード)で開始音が鳴る
- [ ] 日本語で話し、黙って約1.5秒で録音が終わる
- [ ] Claude Desktop(起動済み)に認識テキストが入力され自動送信される
- [ ] 完了音が鳴り、続けてもう一度ウェイクワードに反応する
- [ ] Claude Desktopを終了した状態でも、ウェイク→発話でアプリが起動し送信される

## 注入(Chatタブへの送信)の確認

- [ ] Code/Cowork タブを開いた状態で `vac.check inject "テスト"` を実行 → 自動でChatタブに切り替わり、Chat入力欄にテキストが入って送信される
- [ ] Chatタブを開いた状態でも同様に送信される
- [ ] 送信のたびに新規チャットが開かれ、まっさらな会話に送られる
- [ ] 送信先が必ずChatタブの入力欄であること(Cowork/Codeには入らない)
- [ ] 貼り付け→Enterの間に別ウィンドウへフォーカスを移すと、誤爆せず DeliveryError(エラー音)で中止される
- [ ] inject_settle_s を下げても(例0.15)注入が失敗しないか確認。失敗するなら値を戻す
- [ ] Claude Desktop のウィンドウタイトルが `^Claude(\s.*)?$` にマッチする / 入力欄が唯一のEditとして見える(`vac.check tree` で確認)

## フォーカス安全性(誤送信防止)

- [ ] 貼り付け→ENTER の間(0.3秒)に別ウィンドウへ意図的にフォーカスを移し、
      ENTER が他アプリに飛ばず DeliveryError(エラー音)になることを確認

## 異常系

- [ ] ウェイク後に何も話さない → 約5秒でエラー音、何も送信されない
- [ ] ウェイク後に雑音のみ → エラー音、何も送信されない(送信されたら閾値を調整)
- [ ] 送信前にクリップボードへコピーした内容が、送信後も Ctrl+V で貼り付けられる
- [ ] クリップボードが空の状態で注入 → 送信後もクリップボードが空のまま
      (注入テキストが残らない)
- [ ] クリップボード履歴ツール等が動いている状態で連続注入しても失敗しない
      (OpenClipboard の競合リトライの確認)
- [ ] USBマイクを抜く → 約2秒でエラー音、vac.log にエラーが記録され、
      挿し直すと5秒間隔のリトライで自動復帰する
- [ ] テレビ・音楽を流した状態で30分放置 → 誤送信が起きない(起きたら wake_threshold を上げる)

## マイク切替(トレイ)

- [ ] トレイの「マイク」に入力デバイス一覧が出て、現在の選択にチェックが付く
- [ ] 別のマイクを選ぶと数秒以内に切り替わり(モデル再ロードなし)、以降その入力で動く
- [ ] 再起動しても選んだマイクが維持される(config.toml の input_device に保存)

## 常駐・終了

- [ ] トレイの「終了」でアイドル中・録音中・送信中のどのタイミングでも即終了し、
      ゾンビプロセスが残らない
- [ ] ウェイク→発話→送信のフルサイクルがワーカースレッドで動く
      (UIA が CoInitialize エラーを出さない)
- [ ] 数時間常駐させて vac.log の肥大化が問題ないか確認

## ログ確認

- [ ] `~/.config/voice-activate-claude/vac.log` に各サイクルのINFOログが残っている
- [ ] 注入失敗時、認識テキストがERRORログに残っている
